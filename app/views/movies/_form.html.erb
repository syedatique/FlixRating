<%= form_for(@movie) do |f| %>
  <% if @movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
      <% @movie.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="container">
    <div class="form-group">
      <%= f.label :category_id, 'Decade: ' %>
      <%= f.select :category_id, Category.all.map { |c| [c.decade_name, c.id] }, class: 'form-control' %><br>

      <%= f.label :title, 'Type the Title of one of the best three movies' %>
      <%= f.text_field :title, class: 'form-control' %><br>
      <%= f.submit "Submit", class: 'btn btn-success' %>

    </div>

  <% end %>

</div>
  